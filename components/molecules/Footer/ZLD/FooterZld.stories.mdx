import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs/blocks'
import FooterZld from './'
import ZldStoryProvider from '@/components/common/ZldStoryProvider'

<Meta
  title="Components/Molecules/FooterZld"
  component={FooterZld}
  parameters={{
    docs: {
      description: {
        component: 'Footer component for  zld brand'
      }
    }
  }}
/>

export const Template = () => (
  <ZldStoryProvider>
    <FooterZld />
  </ZldStoryProvider>
)

# FooterZld

Displays the footer component for zld

# Demo

<Canvas>
  <Story name="FooterZld" component={FooterZld}>
    <Template />
  </Story>
</Canvas>
